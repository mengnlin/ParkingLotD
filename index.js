const { ParkingLot, Car } = require("./MainSystem");

const car = new Car();

car.assignTicket();

const human = new human();

class Human {
  read(thing) {
    console.log(thing.toString());
    // thing.read();
  }
}

human.read(book);
